# 20_7 feature contract

Group: `itlibertas_timesheet`, `itlibertas_timesheet_gantt`, `odoo_menu_management`.
Live: 1064 / 1.1.14 Flexible Timetables and Shifts; 1065 / 1.0.5 Gantt; 1063 / 1.0.7 Advanced Menu Management.
Pin: origin/19.0 `d7235473f3a`. Scaffold `971c21a311e`. Seed `ea9dc4a81db`.

## Surfaces (Gx.3 / Gx.8 first-click)

- Timetables menu: templates, current/last week sheets, table widget, New on form/list (`buttonTemplate` on `web.FormView.Buttons` / `web.ListView.Buttons`).
- Shifts + attendance kiosk / systray with extra contexts (H6). Barcode goes through `scan_barcode_with_geolocation`.
- Menu management: hide menus per user (`hide_menu_ids`, `user_writeable=True`).
- Enterprise: Gantt on `day.hours.sample` (`web_gantt`). Community lane omits the gantt module.

## Demo (Gx.4 / Gx.5)

Layer 1: all three `"demo": []` (same as 19.0).
Layer 2: `odootools_demo` `_load_demo_timesheets` — 14 `timetable.timesheet` when all 6 CAST employees resolve, `day.hours.sample` from SHIFTS, settings typed ICP, 0user + Anita `group_omm`, Anita approval + HR manager.

## Checks

- Automated: none (`tests/` empty). Gx.7 is install + first-click.
- Manual Gx.8: Employees → Timetables / Shifts / Work Schedule; Settings → Menu Management; timetable table New; kiosk/systray extra context (H6); hide a menu as demo; enterprise Gantt on Shifts. Filestore filled 2026-09-19 (`env-gx8-filestore.sh gx8_20_7` → `GX8_FILESTORE_OK` + `GX8_REPORT_URL_OK`). Empty print raises UserError. Timetable uses `record.resId`. Menu icons are `bool(icon)`.
- Gx.5 proof: 14 timesheets, 73 shifts, `demo`/`demo`, kiosk on.
- Gx.9: owner finalize 2026-09-19. B1 `b7_26873` green. Merged `1d2297291ea`.
