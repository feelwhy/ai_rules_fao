# `20_15` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `d7235473f3a` (branch `20_15`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_15`). Shared `/home/feelwhy/Odoo/tools`
stays `19.0`.

## Modules and live pages

| Module | Live name | 19.0 `exact_version` |
|---|---|---|
| `knowsystem` | Knowledge Base System | 1.3.35 |
| `knowsystem_website` | Website and Portal | 1.1.17 |
| `knowsystem_multilang` | Multi Languages | 1.0.11 |
| `knowsystem_custom_fields` | Custom Fields | 1.0.6 |
| `knowsystem_website_custom_fields` | Custom Fields for Website and Portal | 1.0.3 |
| `documentation_builder` | Odoo Documentation Builder | 1.1.17 |
| `knowsystem_eshop` | eCommerce | 1.0.7 |

Community + website. No enterprise lane. Layer 2
`odootools_demo` KnowSystem / documentation loaders.

## Surfaces (Gx.3 / Gx.8 first-click)

- Backend: articles, sections, tags, tours, kanban, jstree, revisions, builder
  (html_builder / html_editor). Match `mass_mailing` saas JS for editor plugins.
- Website / portal: `/knowsystem` list, article, likes, jstree JSON.
- Documentation builder website TOC / search / options.
- eShop FAQ on `website_sale` product (split Interactions).
- Article / section access must match 19.0 (user read domain, editor write
  domain, admin TRUE). Do not grant Internal User write.

## Demo (Gx.4 / Gx.5)

Layer 1: module `demo/` as on 19.0. Backend-editor bodies need this serie's
editor re-capture before publish (`35-knowsystem-demo`); Gx.4 keeps the 19.0
arch as input.
Layer 2: `_load_demo_knowsystem` / documentation loaders. Prove 19.0 counts.

## Checks

- Automated: `knowsystem` ordering + editor save; website / documentation /
  eshop HttpCases. Gx.2 = pin (same as `20_11`; do not `--test` shared demo19).
- Manual Gx.8: first-click every KnowSystem / Documentation menu; open builder;
  website article; portal; FAQ on a product; access as demo vs editor vs admin.
