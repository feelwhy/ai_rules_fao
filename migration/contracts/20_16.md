# `20_16` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `6dcca1190cc` (branch `20_16`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_16`). Shared `/home/feelwhy/Odoo/tools`
stays `19.0`.

## Modules and live pages

| Module | Live name | 19.0 `exact_version` |
|---|---|---|
| `business_appointment` | Universal Appointments and Time Reservations | 1.3.33 |
| `business_appointment_website` | Portal and Website | 1.1.24 |
| `business_appointment_sale` | Sales | 1.1.9 |
| `business_appointment_website_sale` | Website Sales | 1.0.9 |
| `business_appointment_custom_fields` | Custom Fields | 1.0.9 |
| `business_appointment_custom_fields_website` | Custom Fields for Website and Portal | 1.0.4 |
| `business_appointment_time_tracking` | Time Tracking | 1.0.3 |
| `business_appointment_hr` | HR Bridge | 1.0.8 |
| `business_appointment_gantt` | Gantt | 1.0.6 |

Enterprise lane: `business_appointment_gantt` depends on `web_gantt`.
Community cell omits gantt. Layer 2 `odootools_demo` appointment loaders.

## Surfaces (Gx.3 / Gx.8 first-click)

- Backend: Appointments, resource types, resources, services, calendar, list/form
  New (`buttonTemplate` on Form/List; calendar uses a standalone template).
- Website / portal: `/appointments` stepper, time slots widget, portal cards
  (`portal.entry`), website Customize (`website-options` + `page_options_hook`).
- Sale / website_sale bridges; custom fields on appointment + website; time
  tracking; HR resource link; enterprise Gantt.
- Slot engine reads `resource.tz`, not `resource.calendar.tz`.

## Demo (Gx.4 / Gx.5)

Layer 1: `business_appointment/demo/demo_catalog.xml` — 24/7 calendar without
`tz` / attendance `name` / `day_period`. Resource mixin still has `tz`.
Layer 2: `_load_demo_appointments` / `_demo_ba_refresh`. Image writes use
`BinaryBytes`. Prove 19.0 counts: types / resources / services / appointments
(past done + upcoming reserved), `demo`/`demo`.

## Checks

- Automated: `business_appointment` + `business_appointment_website` tags
  (`post_install`). Gx.2 = pin (do not `--test` shared demo19).
- Manual Gx.8: first-click every Appointments menu; website stepper; portal
  home cards; Customize; sale order link; Gantt (enterprise); custom fields
  panel. Filestore + `report.url` via `env-gx8-filestore.sh gx8_20_16`.
