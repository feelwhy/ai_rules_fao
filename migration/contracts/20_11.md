# `20_11` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `d7235473f3a` (branch `20_11`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_11`). Shared `/home/feelwhy/Odoo/tools`
stays `19.0`.

## Modules and live pages

| Module | Live name | `module.description` | 19.0 `exact_version` |
|---|---|---|---|
| `low_sales_report` | Low Sales Report | 1014 | 1.0.5 |
| `sales_forecast_by_periods` | Sales Trends and Forecast | 1012 | 1.0.10 |
| `stock_qty_forecast` | Stock Demand Trends and Forecast | 1013 | 1.0.10 |

Community (`sale` / `stock`). No enterprise lane. Layer 2
`_load_demo_forecasts` / `_demo_fc_refresh` stamps groups, Low Sales
settings, and the shared demo SKUs on the Layer 1 series.

`Gx.2`: no `tests/` in any of the three. Baseline is install + `-u` only.
Named-manual forecast / Low Sales UI is `Gx.8`.

Layer 1: both forecast apps ship `demo/demo_forecast.xml` (3 models + 2
series each). `low_sales_report` ships `"demo": []`.

## Claims and checks

| # | Claim | Check |
|---|---|---|
| L1 | Low Sales wizard opens from Sales | install; Gx.8 menu |
| L2 | Default type / period / critical levels come from settings | typed ICP; Gx.8 Settings |
| S1 | Sales Trends client action opens the pivot | install; Gx.8 first-click |
| S2 | Saved series “by Teams” / “Checking Hypothesis” exist | Layer 1 counts; Gx.5 |
| K1 | Stock Demand client action opens the pivot | install; Gx.8 first-click |
| K2 | Saved series “Delivery stats” / “Hypothesis Checking” exist | Layer 1 counts; Gx.5 |
| K3 | Group-by UoM uses `stock.move.uom_id` | field-lit; Gx.8 extra UoM |

## Port-relevant surface

- **ACL**: `ir.model.access.csv` → `ir.access.csv` on all three (product-group
  `crud`, no empty+grouped-domain pair).
- **Python**: typed ICP on Low Sales defaults; attachment `datas` → `raw`
  (bytes, no base64); `stock.move.product_uom` group-by / tools map → `uom_id`.
- **OWL 3**: `useState` → `proxy`; `state.` → `this.state.`; FormView inherit
  xpath `t-component='this.props.Renderer'`. `t-as` / `t-set` aliases stay
  bare (checker skips those names).
- **Chart.js**: saas ships v4.5.0. Leftover Chart-2 `options.xAxes` is
  rewritten; the canvas is destroyed and recreated when switching line/bar.
