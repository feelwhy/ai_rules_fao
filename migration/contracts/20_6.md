# `20_6` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `3270c4fcaf8` (branch `20_6`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_6`, seed `b7c9051a02a`). Shared
`/home/feelwhy/Odoo/tools` stays `19.0`.

## Modules and live pages

| Module | Live name | `module.description` | 19.0 `exact_version` |
|---|---|---|---|
| `product_stock_balance` | Stocks by Locations | 1078 | 1.1.9 |
| `stock_limitation` | Stocks Access Rules | 1079 | 1.0.4 |
| `product_dynamic_code` | Product Dynamic Codes | 1080 | 1.0.5 |

Community-only depends (`sale_stock` / `stock` / `product`). No enterprise lane.

## Coverage reality

No `tests/` in any of the three modules. `Gx.2` / `Gx.7` are install + `-u`.
UI (location hierarchy on the product form, Super Warehouse vs limited user,
dynamic reference button) is named-manual on `Gx.8`.

Layer 1: all three ship `"demo": []`. Layer 2 lives in
`system/odootools_demo` `demo_stock.py` (`_load_demo_stock`): extra warehouses,
Acoustic Bloc Screens qty, 0user Super Warehouse Manager, ICP stamps.

## Claims and checks

| # | Claim | Check |
|---|---|---|
| S1 | Product form shows expandable location hierarchy | install; Gx.8 open a storable product (`useState` → `proxy`, `this.state`) |
| S2 | Forecast button can show all-warehouse total | install; settings ICP `get_int` / `get_bool` |
| S3 | XLS export stores a file | **port**: `datas` → `raw` bytes |
| L1 | Internal user sees only allowed locations | **port**: narrow stock empty `ir.access`; do not grant `base.group_user` write |
| L2 | Super Warehouse Manager sees the company set | super permission ORs the narrow stock rows |
| C1 | Internal reference updates from a rule | install; Gx.8 Update reference on `product_normal_form_view` |

## Port-relevant surface (pre-port checker: 48 findings)

- **ACL**: three `ir.model.access.csv` → `ir.access.csv` (two header-only).
- **Rules**: `stock_limitation` twelve grouped `ir.rule`. saas stock empty
  grouped ACLs would OR a 1:1 port away — update those stock rows and keep
  Super Warehouse as the wide permission. User rows stay `operation=r`.
- **Python**: `get_param` on four SBL ICP keys; `datas` on XLS export.
- **OWL 3**: `useState`; named `t-ref`; bare `state.` / `qtyFormatFloat` /
  `showTotal`.
- **Views**: `product.product_variant_easy_edit_view` is gone. Successor
  `product.product_product_view_form_normalized`. Forecast button structure
  changed (`o_stat_info` columns).
