# `20_4` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `452438adc4a` (branch `20_4`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_4`, seed `6c919fcfbc0`). Shared
`/home/feelwhy/Odoo/tools` stays `19.0`.

## Modules and live pages

| Module | Live name | `module.description` | 19.0 `exact_version` |
|---|---|---|---|
| `odoo_password_manager` | Password Manager | 1015 | 1.3.24 |
| `odoo_password_manager_custom_fields` | Password Manager: Custom Fields | 1016 | 1.0.7 |
| `security_user_roles` | Security User Roles | 1017 | 1.1.12 |
| `task_numbers` | Task Auto Numbering and Search | 1018 | 1.0.6 |

Community (`mail` / `portal` / `project` / `custom_fields`). Custom fields
needs `custom_fields` from `20_port` on Gx.6. No enterprise lane.

`Gx.2` after excluding tests-prep: existing `odoo_password_manager` ordering
suite plus install + `-u` of the four. Named-manual vault / role / search
UI is `Gx.8`.

Layer 1: password manager ships `demo/demo_catalog.xml`; custom fields ships
`demo/demo_types.xml`; roles and task numbers ship `"demo": []`. Layer 2
lives in `system/odootools_demo`.

## Claims and checks

| # | Claim | Check |
|---|---|---|
| P1 | Vault / key list and form open | install; Gx.8 |
| P2 | Name order is lowercase SQL | existing `test_password_ordering` |
| C1 | Custom password types attach fields | install `custom_fields`; Gx.8 |
| R1 | Roles assign groups to users | install; Gx.8 |
| T1 | Task number search / sequence | install; Gx.8 |

## Port-relevant surface

- **ACL**: `ir.model.access.csv` → `ir.access.csv` on all four.
- **OWL 3**: `useState` → `proxy` on password manager.
- **Python**: typed ICP (`get_param` / `set_param`); `datas` vs `raw` if
  attachment writes; `_order_field_to_sql` already on 19.0.
- **JS**: `@t-ref` kanban inherit; `Deferred` if still imported.
