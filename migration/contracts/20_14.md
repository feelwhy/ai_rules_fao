# `20_14` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `57ca6ad2d41` (branch `20_14`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_14`, seed `43082e6873b`). Shared
`/home/feelwhy/Odoo/tools` stays `19.0`. Re-seeded after 19.0 defects 7 and 8.

## Modules and live pages

| Module | Live name | `module.description` | 19.0 `exact_version` | Docs |
|---|---|---|---|---|
| `cloud_base` | Cloud Storage Solutions | 1031 | 1.4.60 | https://faotools.com/docs/9/cloud-storage-solutions-18 |
| `owncloud_odoo` | ownCloud / Nextcloud Odoo Integration | 1032 | 1.1.11 | https://faotools.com/docs/9/owncloud-nextcloud-21 |
| `dropbox` | Dropbox Odoo Integration | 1033 | 3.0.5 | https://faotools.com/docs/9/dropbox-22 |
| `google_drive_odoo` | Google Drive Odoo Integration | 1034 | 1.3.7 | https://faotools.com/docs/9/google-drive-19 |
| `onedrive` | OneDrive / SharePoint Odoo Integration | 1035 | 1.2.8 | https://faotools.com/docs/9/onedrive-sharepoint-20 |
| `cloud_base_documents` | Cloud Sync for Enterprise Documents | 1036 | 1.1.17 | — |
| `amazon_s3_odoo` | Amazon S3 / MinIO Odoo Integration | 1106 | 1.0.2 | — |
| `box_odoo` | Box Odoo Integration | 1108 | 1.0.2 | — |
| `nas_odoo` | NAS / Synology / QNAP / TrueNAS Odoo Integration | 1109 | 1.0.2 | — |

Core is `cloud_base` (`depends: portal`). Each client module adds a `clouds.client`
selection and its HTTP/SDK adapter. `cloud_base_documents` depends on enterprise
`documents` — community `demo19` / `demo20` cannot install it.

## Coverage reality

| Module | Tests | What they prove |
|---|---|---|
| `cloud_base` | `test_clouds_folder.py` | rule folders + cron (`test1`), client compute (`test2`), ACL (`test3`), attachment–folder link (`test4`) |
| `owncloud_odoo` | `test_owncloud_service.py` | mocked WebDAV client |
| `amazon_s3_odoo` | `test_s3_service.py`, `test_minio_client.py` | mocked S3 / MinIO |
| `box_odoo` | `test_box_client.py` | mocked Box client |
| `nas_odoo` | `test_nas_service.py` | mocked NAS / WebDAV |
| `cloud_base_documents` | `test_documents_workspaces.py` | Documents workspace sync (needs `documents`) |
| `google_drive_odoo`, `onedrive`, `dropbox` | none | install / upgrade only |

`Gx.2` / `Gx.7` prove those tests plus install. Live OAuth, real-cloud sync, File
Manager UI, portal shares, and chatter jstree are named manual checks on `Gx.8`.

## Claims and checks

### Core (`cloud_base`)

| # | Claim | Check |
|---|---|---|
| C1 | Sync rules build a folder tree from a model domain | **automated**: `test1_general_cron_triggering` |
| C2 | Nested rules follow `parent_field` and `name_expression` | **automated**: same |
| C3 | File-manager users can work folders; portal cannot invent shares | **automated**: `test3_security_rights_check` |
| C4 | Attachments sit on a `clouds.folder` | **automated**: `test4_attachments_folder_rel_check` |
| C5 | File Manager (jstree + kanban) lists / uploads / tags | manual on Gx.8 |
| C6 | Chatter shows the document folder and URL attachments | manual: chatter box on a partner |
| C7 | Portal share link lists files for the partner | manual: `group_cloud_base_share` + `/my` |
| C8 | Client error / recover posts a subtype | code: `_track_subtype` → `_track_log_get_default_subtype` |

### Client modules

| # | Claim | Check |
|---|---|---|
| P1 | Each client appears on `clouds.client` and can confirm | install + selection; live OAuth is Gx.8 |
| P2 | ownCloud / S3 / MinIO / Box / NAS adapters talk to their API | **automated** mocked service tests |
| P3 | Google Drive / OneDrive / Dropbox adapters exist | no tests — Gx.8 confirm a client form opens |

### Enterprise Documents

| # | Claim | Check |
|---|---|---|
| D1 | A Documents workspace syncs into a cloud folder | **automated**: `test_documents_workspaces.py` on **enterprise** |
| D2 | Documents kanban still shows the cloud badge | manual; OWL `useEffect` → `useLayoutEffect` |

## Port-relevant surface (pre-port checker: 95 findings)

- **ACL**: nine `security/ir.model.access.csv` (seven clients + `cloud_base_documents`
  are header-only). `cloud_base` has real rows, mixed xmlids (`model_clouds_client`
  vs `cloud_base.model_clouds_share`). `Gx.3` → `ir.access.csv`, technical model
  names, `operation` from `perm_*` (`1,1,1,0` → `cru`; `1,0,0,0` → `r`;
  `1,1,0,0` → `ru`).
- **Rules**: two `ir.rule` on `clouds.share`. First lists **two groups** → two
  `ir.access` records. Second is `group_cloud_base_user` + `[(1, '=', 1)]`.
- **Dead hooks**: `_track_subtype` on `clouds.client`; `_inverse_datas` on
  `ir.attachment` (`_inverse_raw` already exists — drop the dead one);
  `_to_store_defaults` → Store `FieldList` family.
- **Python API**: 46 sites — `get_param` / `set_param`, six `registry.clear_cache`
  (`clouds_folder.py` ×4, `sync_model.py` ×2).
- **`datas`**: two controller writes + two form fields (`ir_attachment.xml`).
  Writes are silently dropped if left. `cloud_base_documents` tests still pass
  `datas=` on `documents.document` — confirm that field on saas-19.4.
- **JS**: 11 `useState` → `proxy`; chatter import
  `@mail/chatter/web_portal/chatter` gone; `ensure_jquery` gone (already loads
  jQuery in `onWillStart`); `x2ManyCommands` moved to `@web/core/orm_plugin`.
- **OWL**: `//article[@t-ref='root']` on the File Manager kanban; named `t-ref`
  on logs + upload input → `t-custom-ref`; documents `useEffect(fn, deps)`.
- **Manifests**: drop `19.0.` prefix (tails stay `1.4.60`, `1.1.11`, `3.0.5`,
  `1.3.7`, `1.2.8`, `1.1.17`, `1.0.2` ×3).
- **Layer-1**: none of the nine modules ship `demo/`. Not a plug — File Manager
  is empty until a client/rule exists; that is Layer 2.
- **Layer-2**: `odootools_demo.models.demo_cloud_storage` (and `set_param` in
  that loader). Review only — do not edit `system` 19.0. Do not install
  `odootools_demo` on the saas-19.4 oneshot until `system@20.0` has the ICP
  rename; 19.0 loader `get_param` dies on the stand-in.
