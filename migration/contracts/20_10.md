# `20_10` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `17f1d97163d` (branch `20_10`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_10`, seed `de050173895`). Shared
`/home/feelwhy/Odoo/tools` stays `19.0`.

## Modules and live pages

| Module | Live name | `module.description` | 19.0 `exact_version` |
|---|---|---|---|
| `task_checklist` | Task Check List and Approval Process | 1083 | 1.2.9 |
| `crm_checklist` | CRM Check List and Approval Process | 1082 | 1.2.9 |
| `sale_order_checklist` | Sale Order Approval Check Lists | 1084 | 1.0.4 |
| `purchase_order_checklist` | Purchase Order Approval Check Lists | 1081 | 1.0.5 |

Community-only (`project` / `crm` / `sale` / `purchase`). No enterprise lane.
No `tests/` in any of the four. `Gx.2` / `Gx.7` are install + `-u`. `Gx.7`
hands over the review-DB URL. Toggle / stage-gate UI is named-manual on
`Gx.8` (owner walk of that URL).

Layer 1: each ships `demo/demo_checklists.xml`. Layer 2 lives in
`system/odootools_demo` `demo_checklists.py` (`_load_demo_checklists`).

## Claims and checks

| # | Claim | Check |
|---|---|---|
| T1 | Task form shows stage checkpoints | install; Gx.8 open a New task |
| T2 | Approving writes the many2many SET | **port**: `list.set` |
| C1 | CRM opportunity form shows stage + team checkpoints | Gx.8 |
| S1 | Sale order form shows state + team checkpoints | Gx.8 |
| P1 | Purchase order form shows state + company checkpoints | Gx.8 |
| P2 | Company checklist rows are company-restricted | **port**: `ir.access` restriction |

## Port-relevant surface (pre-port checker: 29 findings)

- **ACL**: four `ir.model.access.csv` → `ir.access.csv`. Sale/purchase keep
  19.0 ops (`r` / `cr` / `crud` — letters stay in `crud` order). Purchase global company `ir.rule` →
  restriction (no `group_id`).
- **OWL 3**: `useState` → `proxy`; `this.state` / `this.props`.
- **JS API**: `StaticList._replaceWith` → `list.set(ids)`.
- **Kanban card_id**: inherit `project.view_task_card`, not the hollow
  `project.view_task_kanban` (`priority` moved).

## Layer-1 / Layer-2 counts (`Gx.4` / `Gx.5`)

- 5 `check.list`
- 6 `crm.check.list`
- 5 `order.check.item`
- 3 `purchase.check.item` + 1 `check.company.list`
