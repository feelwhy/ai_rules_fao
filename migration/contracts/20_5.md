# `20_5` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `6a56483a0bb` (branch `20_5`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_5`, seed `c81c22c5939`). Shared
`/home/feelwhy/Odoo/tools` stays `19.0`.

## Modules and live pages

| Module | Live name | `module.description` | 19.0 `exact_version` |
|---|---|---|---|
| `joint_calendar` | Joint Calendar | 1029 | 1.2.9 |
| `joint_calendar_gantt` | Joint Calendar: Gantt | 1030 | 1.0.4 |
| `kpi_scorecard` | KPI Balanced Scorecard | 1027 | 1.1.12 |
| `total_notify` | Reminder Designer and Periodic Reporting | 1028 | 1.3.10 |

`joint_calendar_gantt` depends on enterprise `web_gantt`. Community `demo19` /
`demo20` cannot install it. The other three depend only on `mail`.

## Coverage reality

No `tests/` in any of the four modules. `Gx.2` / `Gx.7` are install + `-u` plus
the `odootools_demo` loader idempotency cases (`test_joint_calendar_loader_is_idempotent`,
`test_total_notify_loader_is_idempotent`, `test_kpi_scorecard_loader_is_idempotent`).
UI (super calendar, KPI kanban + jstree, reminder send, Gantt) is named-manual on `Gx.8`.

Layer 1: all four ship `"demo": []`. Layer 2 lives in `system/odootools_demo`
(`demo_joint_calendar.py`, `demo_total_notify.py`, `demo_kpi.py`).

## Claims and checks

| # | Claim | Check |
|---|---|---|
| J1 | Rules merge Odoo documents into a joint calendar | install; Layer-2 loader; Gx.8 open a calendar |
| J2 | Event ACL follows the linked document | **port**: `_check_access` → `_access_domain` + `res_access_*` |
| J3 | Enterprise Gantt shows the same events | install on enterprise; Gx.8 |
| K1 | KPI scorecard kanban + formula + history | install; Gx.8 (`useState` → `proxy`, New button on `KanbanView.Buttons`) |
| K2 | KPI jstree filters the kanban | Gx.8; tree payload off `proxy` + state-plugin guard |
| N1 | Reminder cron builds a table and mails it | install; `_prepare_mail_values` (was dead `get_mail_values` on 19.0 too) |
| N2 | XLS / PDF attachments store | **port**: `datas` → `raw` bytes |

## Port-relevant surface (pre-port checker: 110 findings)

- **ACL**: four `ir.model.access.csv` → `ir.access.csv`. `joint_calendar_gantt` is header-only.
- **Rules**: `joint_calendar` one global restriction; `total_notify` two grouped rules;
  `kpi_scorecard` eleven company / access rules (incl. `perm_*` subsets on targets).
- **Python**: `_check_access` on `joint.event`; `get_param` / `set_param`;
  `Registry.clear_cache`; `datas` on KPI export + reminder attachments;
  `get_mail_values` → `_prepare_mail_values`.
- **OWL 3**: 8 `useState`; 62 bare `state.` / `props.` / `getX()`; KPI New-button
  xpath still on `web.KanbanView`; three `loadJS` of core jquery.
- **jstree**: KPI tree keeps `treeData` on `useState` — same 100ms `save_state` hole as File Manager.

## Gx.3 / Gx.6 facts (2026-09-15)

- Calendar view: drop `date_delay` (RNG + `FIELD_ATTRIBUTE_NAMES`).
- Menu cache: `@tools.ormcache` → `@api.ormcache`.
- Gantt popover: `t-esc` → `t-out` in `ir.ui.view` arch (OWL `static/src` may keep `t-esc`).
- Checker kinds added: `calendar-attr`, `qweb-tesc`. Post-port checker: 0 findings.

`Gx.7` community + enterprise: 0 failed. No module `tests/`. Feature walk is `Gx.8`.

## Gx.8 first-click (2026-09-16) — folded into Gx.3, not a walk finding

The checker was green and still let three compile-time OWL holes through:

| Surface | Miss | Successor |
|---|---|---|
| Joint Calendar menu | `class="o_calendar_sidebar" position="inside"` | `//div[hasclass('o_calendar_sidepanel_content')]` + `this.env.isSmall` |
| KPI interface | bare `panelState` / `panelClass` / `panelStyle` | `this.panelState` / `this.panelClass` / `this.panelStyle` |
| Reminder Designer Filters | `update.bind="handleChange"` + `t-on-click="clear"` | `this.handleChange` / `this.clear` |

`20_14` `rule_parent.xml` already had `this.handleChange`. `20_2` has no FieldFilter.
Checker kinds extended the same job.

**`Gx.5` is not green.** Layer-2 was marked done because those three loaders had no
`get_param`. `odootools_demo` still hard-depends on unported apps (`email_suite`,
password manager, …) so it never installed and `_load_demo_joint_calendar` /
`_load_demo_kpi_scorecard` / `_load_demo_total_notify` never ran. The `/tmp` Gx.8
seed (plus a late `demo`/`demo`) is a walk aid, not the 19.0 path. Next: make
`odootools_demo` installable on this stand-in and run those loaders. Do not mark Gx.9.
