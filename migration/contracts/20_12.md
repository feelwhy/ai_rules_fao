# `20_12` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `c06d7622722` (branch `20_12`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_12`, port commit `4c9fdc6a2fc`). Shared
`/home/feelwhy/Odoo/tools` stays `19.0`.

## Modules and live pages

| Module | Live name | `module.description` | 19.0 `exact_version` |
|---|---|---|---|
| `partner_color_codes` | Contacts Color Coding | 1074 | 1.0.10 |
| `activities_todo` | Activities To-Do Interface | 1076 | 1.0.4 |
| `recurrent_activities` | Recurring Activities | 1075 | 1.0.7 |

Community (`base_setup` / `mail`). No enterprise lane. `custom_fields` is not
a depend; `odootools_demo` Layer 2 stamps colors / rules / templates.

`Gx.2`: no `tests/` in any of the three. Baseline is install + `-u` only.
Named-manual color / to-do / template UI is `Gx.8`.

Layer 1: recurrent ships `demo/demo_templates.xml` (Azure weekly, Gemini
yearly). Colors and to-do ship `"demo": []`. Layer 2
`_demo_ca_refresh` / `_load_demo_contacts_activities`.

## Claims and checks

| # | Claim | Check |
|---|---|---|
| C1 | Color codes list and partner form show colors | install; Gx.8 Contacts + partner |
| C2 | Color tooltip on many2one is a real OWL component | checker; Gx.8 hover |
| T1 | Activities To-Do board opens | install; Gx.8 To-Do menu |
| T2 | User preferences `act_type_ids` / `only_old_activities` are self-writable | `user_writeable=True`; Gx.8 Preferences |
| R1 | Recurring templates list opens | install; Gx.8 Recurring menu |
| R2 | Own-only vs admin template ACL (no Internal User write grant) | `ir.access` product groups; Gx.8 |

## Port-relevant surface

- **ACL**: `ir.model.access.csv` → `ir.access.csv` on all three. Recurrent
  19.0 grouped `ir.rule` domains sit on product groups, not
  `base.group_user`.
- **Python**: `SELF_*_FIELDS` gone → `user_writeable=True` on
  `activities_todo` user prefs.
- **OWL 3**: `useState` → `proxy`; ColorCodes tooltip is a Component
  (saas Tooltip `info` is compile-scope; checker `owl-this` would
  otherwise force a wrong `this.`).
- **Views**: partner form dropped `company_type`; list
  `complete_name` → `display_name`; kanban `name`. child_ids still
  has kanban + form; 19.0 `//main/field[@name='name']` is now
  `//kanban//main//field[@name='name']` (name sits in a div).
