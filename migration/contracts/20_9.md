# `20_9` feature contract

`Gx.1` of the 19 → 20 port. Live 19.0 pages on faotools.com, source, and checks.

Seeded at `Gx.0` from `tools@19.0` `3270c4fcaf8` (branch `20_9`, worktree
`/home/feelwhy/Odoo/_worktrees/tools-20_9`, seed `00701e1fca6`). Shared
`/home/feelwhy/Odoo/tools` stays `19.0`.

## Modules and live pages

| Module | Live name | `module.description` | 19.0 `exact_version` |
|---|---|---|---|
| `crm_duplicates` | CRM Duplicates | 1068 | 1.0.4 |
| `partner_duplicates` | Contacts Duplicates | 1070 | 1.0.5 |
| `website_url_translations` | URL Transliterations | 1066 | 1.0.5 |
| `short_urls` | Website URL Shortener | 1067 | 1.0.14 |
| `odoo_typo_reporter` | Typo Reporter | 1099 | 1.0.3 |

Community-only (`crm` / `contacts` / `website` / `mail`). No enterprise lane.

## Coverage reality

Only `odoo_typo_reporter` ships `tests/`. `Gx.2` / `Gx.7` are that suite
plus install + `-u` for the other four. `Gx.7` hands over the review-DB URL.
UI (duplicate smart button, language switch, short-URL dialog, typo modal)
is named-manual on `Gx.8` (owner walk of that URL).

Layer 1: all five ship `"demo": []`. Layer 2 lives in
`system/odootools_demo` `demo_duplicates.py` (`_load_demo_duplicates`).
`partner_duplicates` stays in `DEMO_FORBIDDEN_MODULES` (uninstalled).

## Claims and checks

| # | Claim | Check |
|---|---|---|
| D1 | Contact / lead form shows a Duplicates smart button when count > 0 | install; Gx.8 open Azure extras |
| D2 | Rigid criteria block create/write | install; settings ICP `get_bool` |
| U1 | Language switch uses the transliterated slug when one exists | **port**: patch `LangChange` |
| S1 | Website editor can add short-URL / QR snippets | **port**: `website.snippets` inherit + `group="content"` |
| S2 | Manage Short URLs dialog opens | **port**: `useState` → `proxy`, `t-custom-ref`, `this.` |
| T1 | Public/internal typo submit stores attachments | **port**: JSON `raw` + `ir.attachment.raw`; ICP `get_str` |

## Port-relevant surface (pre-port checker: 55 findings)

- **ACL**: five `ir.model.access.csv` → `ir.access.csv`. No grouped rule
  next to an empty permission. User rows stay `r`; typo public/portal stay `c`.
- **Python**: typed ICP on every `get_param` / `set_param`;
  `content_disposition` from `odoo.http.stream`.
- **OWL 3 / Interaction**: `website_root` include → `LangChange` patch;
  short-URL dialog `proxy` / `t-custom-ref` / `this.`.
- **Views**: `type_person` is gone (`inactive` successor).
  `website.snippets` still exists as a `<template id>` (checker now indexes it).

## Layer-2 counts (`Gx.5`)

After `_load_demo_duplicates` on a DB that has the four allowed modules:

- 2 extra Azure partners (`demo_dup_partner_azure_interiors`, `_llc`)
- 2 extra Azure leads
- 3 short URLs including token `chairs`
- 3 `website.error.report` rows
- `partner_duplicates` not installed
- URL translations only if `ru_RU` / `fr_FR` are active (review DB is `en_US`)
